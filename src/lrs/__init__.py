"""Learning Record Store — xAPI ingestion, statement compression, and graph analytics.

Role-per-command, one image (ADR-005): `lrs gateway`, `lrs processor`, `lrs bootstrap`, …
The role is an argument, not a build.
"""

__version__ = "0.1.0"
